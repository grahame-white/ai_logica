#!/bin/bash

# script/server: Launch the application and any extra required processes
#                locally.

set -e

cd "$(dirname "$0")/.."

echo "==> Starting AI Logica server..."

# Navigate to the web project and start the server
cd AiLogica
exec dotnet run