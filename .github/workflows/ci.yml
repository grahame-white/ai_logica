---
name: CI

on:
  push:
    branches: [main]
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '**/*.sln'
      - '.github/workflows/**'
      - 'script/**'
  pull_request:
    branches: [main]
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '**/*.sln'
      - '.github/workflows/**'
      - 'script/**'

# Cancel superseded workflow runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Principle of least permissions
permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup .NET
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4.2.3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Check code formatting
        run: dotnet format --verify-no-changes --verbosity normal

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run unit and integration tests
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "Category!=EndToEnd"

  end-to-end-tests:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup .NET
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4.2.3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Install system Firefox and setup virtual display
        run: |
          sudo apt-get update
          sudo apt-get install -y firefox xvfb
          
      - name: Start virtual display
        run: |
          export DISPLAY=:99
          Xvfb :99 -screen 0 1280x720x24 &
          sleep 2

      - name: Install Playwright dependencies (fallback support)
        continue-on-error: true
        run: >
          pwsh AiLogica.Tests/bin/Release/net8.0/playwright.ps1
          install firefox

      - name: Run end-to-end tests
        env:
          DISPLAY: :99
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "Category=EndToEnd"

      - name: Run E2E infrastructure tests (fallback)
        if: failure()
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "EndToEndInfrastructureTests"

      - name: Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-screenshots
          path: /tmp/*.png
          retention-days: 7
