---
name: CI

on:
  push:
    branches: [main]
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '**/*.sln'
      - '.github/workflows/**'
      - 'script/**'
  pull_request:
    branches: [main]
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '**/*.sln'
      - '.github/workflows/**'
      - 'script/**'

# Cancel superseded workflow runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Principle of least permissions
permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup .NET
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4.2.3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Check code formatting
        run: dotnet format --verify-no-changes --verbosity normal

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run unit and integration tests
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "Category!=EndToEnd"

  end-to-end-tests:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup .NET
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4.2.3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Install Playwright browsers
        id: browser-install
        continue-on-error: true
        run: >
          pwsh AiLogica.Tests/bin/Release/net8.0/playwright.ps1
          install firefox

      - name: Run end-to-end tests
        if: steps.browser-install.outcome == 'success'
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "Category=EndToEnd"

      - name: Skip E2E tests notification
        if: steps.browser-install.outcome == 'failure'
        run: |
          echo "::warning::E2E tests skipped due to browser installation failure."
          echo "This is likely due to network restrictions on Playwright downloads."

      - name: Run E2E infrastructure tests (fallback)
        if: steps.browser-install.outcome == 'failure'
        run: >
          dotnet test --no-build --no-restore --configuration Release
          --verbosity normal --filter "EndToEndInfrastructureTests"

      - name: Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-screenshots
          path: /tmp/*.png
          retention-days: 7
